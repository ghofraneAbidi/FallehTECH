<!DOCTYPE html>

<title>Modifier Like</title>

{% block body %}
    <h1>Edit Like</h1>

    {{ include('like/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_like_index') }}">Retour a la liste</a>

    {{ include('like/_delete_form.html.twig') }}
{% endblock %}

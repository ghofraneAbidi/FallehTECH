<form method="POST" action="{{ path('app_user_delete_front') }}">
    <input type="hidden" name="token" value="{{ csrf_token('delete_account') }}">
    <button type="submit" class="btn text-danger">Supprimer mon compte</button>
</form>